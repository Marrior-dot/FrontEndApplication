<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body class="container">
<main>
  <div class="jumbotron">
    <%- include ("./partials/quantity.ejs") -%>

    <% if(quantity > 0){ %>
        <% for(int i = 0; i < quantity; i++) {%>
            <%- include ("./partials/quantity.ejs") -%>
        <% } %>
    <% } %>    
  </div>
</main>
</body>
</html>